The program is written in python language. Single stepping and which program to run is taken from user 
via console. The given search trees are implemented in the code. The last tree is user defined. Therefore,
it prints the output for given trees and then asks the user for the values of user defined tree.